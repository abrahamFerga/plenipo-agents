node_modules/
.DS_Store
*.log
.claude/settings.local.json
# Written by `gh aw init`; it only toggles Copilot for markdown, which is a personal editor
# preference rather than a repo convention.
.vscode/
