# Replace <owner> with the GitHub login read from workflow.json or `gh api user` — never a hardcoded
# name, or this file works for exactly one account.
#
# CODEOWNERS is the human-visibility half of the spine policy: it puts a named person on the review
# of anything touching the five guarantees the platform exists to provide. The ENFORCING half is
# `pr-gates.mjs`, which fails as a required status check — CODEOWNERS only requests a review, and
# only blocks if you also turn on "Require review from Code Owners" in branch protection.
#
# Keep both. The check is what stops an agent; the ownership is what tells you it happened.

* @<owner>

# The spine — RBAC before the model, approval-first writes, tenant isolation, audit, secrets.
/src/**/Authorization/     @<owner>
/src/**/Approvals/         @<owner>
/src/**/Persistence/       @<owner>

# The harness itself. An agent that can edit its own gates has no gates.
/.github/                  @<owner>
/CODEOWNERS                @<owner>
/nuget.config              @<owner>
/workflow.json             @<owner>
